import 'package:flutter/material.dart';

class UseBloc extends StatelessWidget {
  const UseBloc({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
